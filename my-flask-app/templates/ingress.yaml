{{- if .Values.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "my-flask-app.fullname" . }}
  labels:
    app: {{ include "my-flask-app.name" . }}
  annotations:
    {{- range $key, $value := .Values.ingress.annotations }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
spec:
  ingressClassName: {{ .Values.ingress.className }}
  rules:
    - host: {{ .Values.ingress.hosts[0].host | quote }}  # Ensure the host is quoted
      http:
        paths:
          - path: {{ .Values.ingress.hosts[0].paths[0].path | quote }}
            pathType: {{ .Values.ingress.hosts[0].paths[0].pathType | quote }}  # Ensure pathType is quoted
            backend:
              service:
                name: {{ include "my-flask-app.fullname" . }}
                port:
                  number: {{ .Values.service.port }}
{{- end }}
